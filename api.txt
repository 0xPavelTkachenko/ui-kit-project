tick-box: {
  classname,
  modes: ['color-red']
}

toggle: {
  classname,
  modes: ['color-red']
}

btn: {
  classname,
  text,
  type,
  modes: ['size-small', 'color-red']
}

arrow-btn: {
  classname,
  modes: ['transform-right', 'state-disabled', 'size-small']
}

video: {
  classname,
  src
}

user-profile: {
  classname,
  photo,
  facebook,
  twitter,
  dribbble,
  modes: ['color-red']
}

speech-bubble: {
  classname,
  modes: ['color-red', 'color-gray', 'color-white', 'transform-top']
}

feedback-form: {
  classname,
  action,
  method,
  successMsg,
  errorMsg,
  name: {required, placeholder, value},
  email: {required, placeholder, value},
  message: {required, placeholder, value},
  btn: btn component
}

slider: {
  classname,
  modes: ['color-red', 'state-no-bar'],
  min,
  max,
  grid,
  valueLabel,
  minMaxLabel,
  start,
  step
}

dropdown: {
  classname,
  modes: ['color-red'],
  options: [strings],
  placeholder,
  emptyMsg
}

search: {
  classname,
  modes: ['color-red', 'state-not-found'],
  placeholder,
  notFoundMsg,
  value,
  maxLength
}

news: {
  classname,
  modes: ['color-red'],
  title,
  date,
  description,
  img: {src, size, x, y}
}

event: {
  classname,
  modes: ['color-red'],
  day,
  month,
  title,
  description
}

percentage-circle: {
  classname,
  modes: ['color-red']
}

pie-chart: {
  classname,
  data: [{value, color}]
}

info-block: {
  classname,
  modes: ['color-red'],
  img: {src, size, x, y}
}

progress-tracker: {
  classname,
  modes: ['color-red'],
  stepCount,
  activeStep
}

map: {
  classname,
  modes: ['color-red'],
  title,
  address,
  center: {lat, lng},
  marker: {lat, lng},
  showMarker,
  zoom
}

message-form: {
  classname,
  modes: ['color-red'],
  photo: {src, size, x, y},
  name,
  message,
  btn: btn component
}
