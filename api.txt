tick-box: {
  classname,
  modes: ['red']
}

toggle: {
  classname,
  modes: ['red']
}

btn: {
  classname,
  text,
  type,
  modes: ['small', 'red']
}

arrow-btn: {
  classname,
  modes: ['right', 'disabled', 'small']
}

video: {
  classname,
  src
}

user-profile: {
  classname,
  photo,
  facebook,
  twitter,
  dribbble,
  modes: ['red']
}

speech-bubble: {
  classname,
  modes: ['red', 'gray', 'white', 'top']
}

feedback-form: {
  classname,
  action,
  method,
  successMsg,
  errorMsg,
  name: {required, placeholder, value},
  email: {required, placeholder, value},
  message: {required, placeholder, value},
  btn: btn component
}

slider: {
  classname,
  modes: ['red', 'no-bar'],
  min,
  max,
  grid,
  valueLabel,
  minMaxLabel,
  start,
  step
}

dropdown: {
  classname,
  modes: ['red'],
  options: [strings],
  placeholder,
  emptyMsg
}

search: {
  classname,
  modes: ['red', 'not-found'],
  placeholder,
  notFoundMsg,
  value,
  maxLength
}

news: {
  classname,
  modes: ['red'],
  title,
  date,
  description,
  img: {src, size, x, y}
}

percentage-circle: {
  classname,
  modes: ['red']
}

pie-chart: {
  classname,
  data: [{value, color}]
}

info-block: {
  classname,
  modes: ['red'],
  img: {src, size, x, y}
}

progress-tracker: {
  classname,
  modes: ['red'],
  stepCount,
  activeStep
}

map: {
  classname,
  modes: ['red'],
  title,
  address,
  center: {lat, lng},
  marker: {lat, lng},
  showMarker,
  zoom
}
