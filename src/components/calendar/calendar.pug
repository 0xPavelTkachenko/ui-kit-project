mixin calendar(options)
  -
    options = options || {};
    options.color = typeof options.color === "string" ? "calendar_color_" + options.color : undefined;

  .calendar.js-calendar(class=options.color)
