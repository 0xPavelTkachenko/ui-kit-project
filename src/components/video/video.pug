mixin video(options)
  -
    options = options || {};
    options.src = options.src || 'https://www.youtube.com/watch?v=jNQXAC9IVRw';

    options.src = options.src.replace('watch?v=', 'embed/') + '?rel=0';

  iframe.video(
    class=options.classname,
    src=options.src,
    frameborder="0",
    allow="autoplay; encrypted-media",
    allowfullscreen
  )
